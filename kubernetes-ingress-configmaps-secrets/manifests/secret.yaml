# A Secret holds sensitive values. stringData lets me write them in plain text
# and Kubernetes stores them base64 encoded.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
stringData:
  DB_USER: "prateek"
  DB_PASSWORD: "devops123"
