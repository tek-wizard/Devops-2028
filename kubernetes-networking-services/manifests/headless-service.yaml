# A headless Service has no cluster IP, so DNS returns the Pod IPs directly.
apiVersion: v1
kind: Service
metadata:
  name: web-headless
spec:
  clusterIP: None
  selector:
    app: web-app
  ports:
    - port: 80
